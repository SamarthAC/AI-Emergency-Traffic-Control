# Leave unset to run entirely on local mock data + the local animation loop.
# Once the FastAPI/SUMO/AI backend is ready, set this to enable live data:
# VITE_WS_URL=ws://localhost:8000/ws/simulation
